# 통합 개발 환경용 Docker Compose
# 모든 서비스를 하나의 네트워크에서 실행

x-common-env: &common-env
  ES_HOST: host.docker.internal
  ES_PORT: ${ES_PORT:-54321}
  ES_SCHEME: ${ES_SCHEME:-http}
  ES_USERNAME: ${ES_USERNAME:-}
  ES_PASSWORD: ${ES_PASSWORD:-}
  TZ: Asia/Seoul

x-pharm-crawler-env: &pharm-crawler-env
  ES_HOST: host.docker.internal
  ES_PORT: ${ES_PORT:-54321}
  DATA_GO_KR_API_KEY: ${DATA_GO_KR_API_KEY:-}
  TZ: Asia/Seoul

networks:
  dwh-network:
    driver: bridge

services:
  # ============================================================
  # MediDB - Airflow Services
  # ============================================================
  airflow-postgres:
    image: postgres:15
    container_name: dwh-airflow-postgres
    environment:
      POSTGRES_USER: airflow
      POSTGRES_PASSWORD: airflow
      POSTGRES_DB: airflow
    volumes:
      - airflow-postgres-data:/var/lib/postgresql/data
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "airflow"]
      interval: 5s
      retries: 5

  airflow-init:
    image: apache/airflow:2.7.0
    container_name: dwh-airflow-init
    depends_on:
      airflow-postgres:
        condition: service_healthy
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@airflow-postgres/airflow
      AIRFLOW__CORE__FERNET_KEY: ''
      AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
    networks:
      - dwh-network
    entrypoint: /bin/bash
    command:
      - -c
      - |
        airflow db init
        airflow users create \
          --username admin \
          --password admin \
          --firstname Admin \
          --lastname User \
          --role Admin \
          --email admin@example.com || true

  airflow-webserver:
    image: apache/airflow:2.7.0
    container_name: dwh-airflow-webserver
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    ports:
      - "8080:8080"
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@airflow-postgres/airflow
      AIRFLOW__CORE__FERNET_KEY: ''
      AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
      AIRFLOW__WEBSERVER__SECRET_KEY: 'medi-db-secret-key'
    volumes:
      - ./medi-db/airflow/dags:/opt/airflow/dags
      - ./medi-db/airflow/plugins:/opt/airflow/plugins
      - airflow-logs:/opt/airflow/logs
    networks:
      - dwh-network
    command: webserver
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 5

  airflow-scheduler:
    image: apache/airflow:2.7.0
    container_name: dwh-airflow-scheduler
    depends_on:
      airflow-init:
        condition: service_completed_successfully
    environment:
      AIRFLOW__CORE__EXECUTOR: LocalExecutor
      AIRFLOW__DATABASE__SQL_ALCHEMY_CONN: postgresql+psycopg2://airflow:airflow@airflow-postgres/airflow
      AIRFLOW__CORE__FERNET_KEY: ''
      AIRFLOW__CORE__LOAD_EXAMPLES: 'false'
    volumes:
      - ./medi-db/airflow/dags:/opt/airflow/dags
      - ./medi-db/airflow/plugins:/opt/airflow/plugins
      - airflow-logs:/opt/airflow/logs
    networks:
      - dwh-network
    command: scheduler

  # ============================================================
  # MediDB - MSA Services
  # ============================================================
  medi-db-crawler:
    build:
      context: ./medi-db
      dockerfile: services/crawler/Dockerfile
    container_name: dwh-medi-db-crawler
    ports:
      - "8001:8000"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  medi-db-preprocessor:
    build:
      context: ./medi-db
      dockerfile: services/preprocessor/Dockerfile
    container_name: dwh-medi-db-preprocessor
    ports:
      - "8002:8000"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  medi-db-clustering:
    build:
      context: ./medi-db
      dockerfile: services/clustering/Dockerfile
    container_name: dwh-medi-db-clustering
    ports:
      - "8003:8000"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  medi-db-forecasting:
    build:
      context: ./medi-db
      dockerfile: services/forecasting/Dockerfile
    container_name: dwh-medi-db-forecasting
    ports:
      - "8004:8000"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  medi-db-targeting:
    build:
      context: ./medi-db
      dockerfile: services/targeting/Dockerfile
    container_name: dwh-medi-db-targeting
    ports:
      - "8005:8000"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    depends_on:
      - medi-db-clustering
      - medi-db-forecasting
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  medi-db-dashboard:
    build:
      context: ./medi-db
      dockerfile: services/dashboard/Dockerfile
    container_name: dwh-medi-db-dashboard
    ports:
      - "8501:8501"
    environment:
      <<: *common-env
    volumes:
      - ./medi-db/shared:/app/shared
    networks:
      - dwh-network
    depends_on:
      - medi-db-targeting
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============================================================
  # PharmDB - Crawler Service
  # ============================================================
  pharm-crawler:
    build:
      context: ./pharm-db
      dockerfile: Dockerfile
    container_name: dwh-pharm-crawler
    environment:
      <<: *pharm-crawler-env
    volumes:
      - ./pharm-db/crawler:/app/crawler
    networks:
      - dwh-network
    # 일회성 실행 - 수동으로 시작
    profiles:
      - crawler

  # ============================================================
  # Monitoring Service
  # ============================================================
  monitoring:
    build:
      context: ./monitoring
      dockerfile: Dockerfile
    container_name: dwh-monitoring
    ports:
      - "8888:8888"
    environment:
      <<: *common-env
    networks:
      - dwh-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8888/health"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  airflow-postgres-data:
  airflow-logs:
